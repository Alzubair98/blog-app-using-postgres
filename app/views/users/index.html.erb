<% @users.each do |user|%>
<div>
  <img src='#{ <%= user.photo %> }' width='100' height='100' />
  <span><%= user.name%></span>
  <p>number of posts: <%=user.posts_counter || 0%></p>
  <hr />
</div>
<%end%>